IllamHelp local infrastructure was installed in namespace {{ .Release.Namespace }}.

Check dependency status:
  kubectl get pods -n {{ .Release.Namespace }} -l app.kubernetes.io/instance={{ .Release.Name }}

This chart expects Secret {{ include "illamhelp-local-infra.secretName" . }} to exist when secrets.create=false.
