# UI Tokens

Single source of truth for IllamHelp UI tokens (colors, spacing, radii, typography).

## Usage

Web: import the CSS variables file in `globals.css`:

```css
@import "@illamhelp/ui-tokens/web.css";
```

Mobile: import JSON tokens:

```ts
import tokens from "@illamhelp/ui-tokens/tokens.json";
```
